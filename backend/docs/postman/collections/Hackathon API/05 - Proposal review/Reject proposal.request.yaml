$kind: http-request
description: "Planned; returns 404 until implemented. Reject proposal. See backend/docs/api-request-list.md for the contract."
url: "{{base_url}}/api/proposals/{{proposal_id}}/reject"
method: POST
headers:
  "Accept": "application/json"
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('HTTP 200', () => pm.response.to.have.status(200));
      pm.test('JSON response', () => pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json'));
      if (pm.response.code === 200) {
      pm.test('Rejected manually', () => pm.expect(pm.response.json().proposal.status).to.eql('rejected'));
      }
order: 3000
